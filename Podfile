# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'mockstagram' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for mockstagram
  pod 'Firebase/Core'
  pod 'Firebase/Storage'
  pod 'Firebase/Firestore'
  pod 'Kingfisher'            # image handling library
  
  target 'mockstagramTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'mockstagramUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
